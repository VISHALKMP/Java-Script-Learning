var index = 0;
while (index <=20) {
    // console.log(`value of index is ${index}`);
   index = index+2;
}

let Arrays = ['hero','sherr','batman','pokemon']
let arr = 0;
while( arr <Arrays.length){
      console.log(`value is ${Arrays[arr]}`)
      arr = arr+1
}